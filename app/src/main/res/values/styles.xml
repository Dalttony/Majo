<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
       <item name="colorPrimary">@color/majo_500</item>
        <item name="colorPrimaryDark">@color/majo_700</item>
        <item name="colorAccent">@color/majo_acent200</item>
        <item name="android:windowActionBarOverlay">true</item>

    </style>

</resources>
